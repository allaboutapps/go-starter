kind: Changed
body: 'Moved the auth related parts from `internal/api/auth` to `internal/auth`. **BREAKING**: Changes to `authentication_result.go`, `context.go` and `scopes.go` need to be reflected in `internal/auth` instead of `internal/api/auth` and imports need to be updated'
time: 2025-03-18T13:33:19.099047541Z
